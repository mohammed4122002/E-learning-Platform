import { PageSkeleton } from "@/components/ui/PageSkeleton";

export default function ProgramsLoading() {
  return <PageSkeleton title="برامجي" subtitle="المنتج التعليمي — تُنشأ منه الدورات" layout="list" />;
}
