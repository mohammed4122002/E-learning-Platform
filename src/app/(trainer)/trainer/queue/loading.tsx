import { PageSkeleton } from "@/components/ui/PageSkeleton";

export default function Loading() {
  return <PageSkeleton title="بانتظار إجرائي" subtitle="كل ما يحتاج قرارًا أو إجراءً منك" layout="list" />;
}
