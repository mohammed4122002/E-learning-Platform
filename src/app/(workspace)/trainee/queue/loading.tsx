import { PageSkeleton } from "@/components/trainings/RouteStates";

export default function Loading() {
  return <PageSkeleton title="بانتظار إجرائي" subtitle="كل ما يحتاج قرارًا أو إجراءً منك" />;
}
