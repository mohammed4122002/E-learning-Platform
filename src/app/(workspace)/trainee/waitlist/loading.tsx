import { PageSkeleton } from "@/components/trainings/RouteStates";

export default function Loading() {
  return <PageSkeleton title="قائمة انتظاري" subtitle="الدورات المكتملة التي تنتظر شغور مقعد فيها" />;
}
